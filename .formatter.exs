# Used by "mix format"
[
  locals_without_parens: [
    remote_run: 2,
    remote_run: 3,
    assert_peer_receive: 2,
    refute_peer_receive: 2
  ],
  inputs: ["{mix,.formatter}.exs", "{config,lib,test}/**/*.{ex,exs}"]
]
